<!DOCTYPE html>
<html lang="en">
<head>
    /* styles.css - External stylesheet */
/* External style for #heading */
#heading {
  color: blue; /* Lowest priority, will be overridden by inline and internal */
}

  <meta charset="UTF-8">
  <title>CSS Example</title>

  <!-- External CSS (lowest priority of the three) -->
  <link rel="stylesheet" href="styles.css">

  <!-- Internal CSS (in <style> tag) -->
  <style>
    /* 3) Commenting inside CSS class */
    .my-class {
      /* This class sets font and background */
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
    }

    /* Internal style for #heading */
    #heading {
      color: green; /* Will be overridden by inline style */
    }

    /* 4) Using ID, class, and element selectors */
    #myDiv {
      border: 2px solid blue;
      padding: 10px;
    }

    .highlight {
      background-color: yellow;
    }

    p {
      font-size: 18px;
      color: teal;
    }

    /* 5) Group selector for #first and #second */
    #first, #second {
      color: purple;
      padding: 5px;
      border: 1px solid black;
      margin-top: 10px;
    }
  </style>
</head>
<body>

  <!-- 1) Heading with red text using inline style (highest priority) -->
  <h1 style="color: red;">1) Heading with red color (inline)</h1>

  <!-- 2) Div using inline, internal, and external CSS to observe priority -->
  <div id="heading" style="color: orange;">
    2) This div (#heading) uses inline (orange), internal (green), and external (blue) CSS.
  </div>

  <!-- 3 & 4) Demonstrating class, id, and element selectors -->
  <div id="myDiv" class="my-class">
    4) This is a div with ID and class selectors.
    <p>This is a paragraph styled by element selector.</p>
    <h2 class="highlight">This is a heading with class selector</h2>
  </div>

  <!-- 5) Group selector -->
  <div id="first">5) First div using group selector</div>
  <div id="second">5) Second div using group selector</div>

</body>
</html>
